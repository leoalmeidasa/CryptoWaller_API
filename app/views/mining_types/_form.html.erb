<%= simple_form_for @mining_type do |form| %>
  <% if mining_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mining_type.errors.count, "error") %> prohibited this mining_type from being saved:</h2>

      <ul>
        <% mining_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :description, class: "col-sm-2 col-form-label"  %>
    <div class="col-sm-10">
      <%= form.input_field :description, placeholder:"Power of Work" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label :acronym, class: "col-sm-2 col-form-label"  %>
    <div class="col-sm-10">
      <%= form.input_field :acronym, placeholder:"PoW" %>
    </div>
  </div>

  <div class="form-group-row">
    <%= form.submit class:"btn btn-primary" %>
  </div>
  <br>
<% end %>
